============================================================ src/app/models.ts ============================================================ export interface LoginResponse { access_token: string; token_type: string; } export interface User { id: string; email: string; } export interface Thread { id: string; title: string; created_at: string; updated_at: string; } export interface ChatMessage { id?: string; role: 'user' | 'assistant'; content: string; created_at?: string; } ============================================================ src/app/core/auth.service.ts ============================================================ import { Injectable, inject } from '@angular/core'; import { HttpClient } from '@angular/common/http'; import { Router } from '@angular/router'; import { Observable, tap } from 'rxjs'; import { LoginResponse, User } from '../models'; @Injectable({ providedIn: 'root' }) export class AuthService { private http = inject(HttpClient); private router = inject(Router); private apiUrl = 'http://localhost:8000'; login( email: string, password: string ): Observable<LoginResponse> { return this.http.post<LoginResponse>( `${this.apiUrl}/auth/login`, { email, password } ).pipe( tap(response => { localStorage.setItem( 'access_token', response.access_token ); }) ); } register( email: string, password: string ): Observable<LoginResponse> { return this.http.post<LoginResponse>( `${this.apiUrl}/auth/register`, { email, password } ).pipe( tap(response => { localStorage.setItem( 'access_token', response.access_token ); }) ); } getToken(): string | null { return localStorage.getItem( 'access_token' ); } isLoggedIn(): boolean { return !!this.getToken(); } getMe(): Observable<User> { return this.http.get<User>( `${this.apiUrl}/auth/me` ); } logout(): void { localStorage.removeItem( 'access_token' ); this.router.navigate( ['/login'] ); } } ============================================================ src/app/core/auth.interceptor.ts ============================================================ import { HttpInterceptorFn } from '@angular/common/http'; export const authInterceptor: HttpInterceptorFn = (req, next) => { const token = localStorage.getItem( 'access_token' ); if (!token) { return next(req); } const authReq = req.clone({ setHeaders: { Authorization: `Bearer ${token}` } }); return next(authReq); }; ============================================================ src/app/core/auth.guard.ts ============================================================ import { inject } from '@angular/core'; import { CanActivateFn, Router } from '@angular/router'; export const authGuard: CanActivateFn = () => { const router = inject(Router); const token = localStorage.getItem( 'access_token' ); if (token) { return true; } return router.createUrlTree( ['/login'] ); }; ============================================================ src/app/services/thread.service.ts ============================================================ import { Injectable, inject } from '@angular/core'; import { HttpClient } from '@angular/common/http'; import { Observable } from 'rxjs'; import { Thread, ChatMessage } from '../models'; @Injectable({ providedIn: 'root' }) export class ThreadService { private http = inject(HttpClient); private apiUrl = 'http://localhost:8000'; getThreads(): Observable<Thread[]> { return this.http.get<Thread[]>( `${this.apiUrl}/threads` ); } createThread(): Observable<Thread> { return this.http.post<Thread>( `${this.apiUrl}/threads`, {} ); } getMessages( threadId: string ): Observable<ChatMessage[]> { return this.http.get<ChatMessage[]>( `${this.apiUrl}/threads/${threadId}/messages` ); } deleteThread( threadId: string ): Observable<any> { return this.http.delete( `${this.apiUrl}/threads/${threadId}` ); } } ============================================================ src/app/services/chat.service.ts ============================================================ import { Injectable, inject } from '@angular/core'; import { HttpClient } from '@angular/common/http'; import { Observable } from 'rxjs'; export interface ChatResponse { thread_id: string; message: string; } @Injectable({ providedIn: 'root' }) export class ChatService { private http = inject(HttpClient); private apiUrl = 'http://localhost:8000'; sendMessage( threadId: string, message: string ): Observable<ChatResponse> { return this.http.post<ChatResponse>( `${this.apiUrl}/chat`, { thread_id: threadId, message } ); } } ============================================================ src/app/login/login.component.ts ============================================================ import { Component, inject } from '@angular/core'; import { FormsModule } from '@angular/forms'; import { Router } from '@angular/router'; import { AuthService } from '../core/auth.service'; @Component({ selector: 'app-login', standalone: true, imports: [ FormsModule ], templateUrl: './login.component.html', styleUrl: './login.component.css' }) export class LoginComponent { private auth = inject(AuthService); private router = inject(Router); email = ''; password = ''; error = ''; loading = false; registerMode = false; submit(): void { this.error = ''; this.loading = true; const request = this.registerMode ? this.auth.register( this.email, this.password ) : this.auth.login( this.email, this.password ); request.subscribe({ next: () => { this.loading = false; this.router.navigate( ['/chat'] ); }, error: error => { this.loading = false; this.error = error?.error?.detail ?? 'Authentication failed'; } }); } toggleMode(): void { this.registerMode = !this.registerMode; this.error = ''; } } ============================================================ src/app/login/login.component.html ============================================================ <div class="login-page"> <div class="login-card"> <div class="logo"> ∑ </div> <h1> Neural Math Assistant </h1> <p class="subtitle"> {{ registerMode ? 'Create your account' : 'Sign in to continue' }} </p> @if (error) { <div class="error"> {{ error }} </div> } <form (ngSubmit)="submit()" > <label> Email </label> <input type="email" name="email" [(ngModel)]="email" placeholder="you@example.com" required /> <label> Password </label> <input type="password" name="password" [(ngModel)]="password" placeholder="Password" required /> <button type="submit" [disabled]="loading" > {{ loading ? 'Please wait...' : registerMode ? 'Create Account' : 'Login' }} </button> </form> <button class="switch" type="button" (click)="toggleMode()" > {{ registerMode ? 'Already have an account? Login' : 'Create a new account' }} </button> </div> </div> ============================================================ src/app/login/login.component.css ============================================================ .login-page { min-height: 100vh; display: flex; align-items: center; justify-content: center; background: linear-gradient( 135deg, #111827, #1e293b ); } .login-card { width: 400px; padding: 40px; border-radius: 18px; background: #ffffff; box-shadow: 0 25px 60px rgba(0, 0, 0, .3); } .logo { width: 55px; height: 55px; border-radius: 14px; background: #111827; color: white; display: flex; align-items: center; justify-content: center; font-size: 30px; margin-bottom: 20px; } h1 { margin: 0; color: #111827; } .subtitle { color: #64748b; margin-bottom: 30px; } label { display: block; margin: 18px 0 7px; font-weight: 600; } input { width: 100%; box-sizing: border-box; padding: 13px; border: 1px solid #cbd5e1; border-radius: 9px; font-size: 15px; } form button { width: 100%; margin-top: 25px; padding: 13px; border: 0; border-radius: 9px; background: #111827; color: white; cursor: pointer; font-size: 15px; } form button:disabled { opacity: .6; } .switch { width: 100%; border: 0; background: transparent; margin-top: 20px; color: #2563eb; cursor: pointer; } .error { padding: 10px; border-radius: 8px; background: #fee2e2; color: #991b1b; } ============================================================ src/app/chat/chat.component.ts ============================================================ import { Component, OnInit, inject, ChangeDetectorRef } from '@angular/core'; import { FormsModule } from '@angular/forms'; import { CommonModule } from '@angular/common'; import { Router } from '@angular/router'; import { Thread, ChatMessage } from '../models'; import { ThreadService } from '../services/thread.service'; import { ChatService } from '../services/chat.service'; import { AuthService } from '../core/auth.service'; @Component({ selector: 'app-chat', standalone: true, imports: [ CommonModule, FormsModule ], templateUrl: './chat.component.html', styleUrl: './chat.component.css' }) export class ChatComponent implements OnInit { private threadService = inject(ThreadService); private chatService = inject(ChatService); private auth = inject(AuthService); private router = inject(Router); private cdr = inject(ChangeDetectorRef); threads: Thread[] = []; messages: ChatMessage[] = []; currentThreadId: string | null = null; currentThreadTitle = 'New Chat'; messageText = ''; loading = false; loadingThreads = false; userEmail = ''; ngOnInit(): void { this.loadUser(); this.loadThreads(); } loadUser(): void { this.auth.getMe().subscribe({ next: user => { this.userEmail = user.email; } }); } loadThreads(): void { this.loadingThreads = true; this.threadService .getThreads() .subscribe({ next: threads => { this.threads = threads; this.loadingThreads = false; if ( threads.length > 0 ) { this.selectThread( threads[0] ); } else { this.createNewChat(); } }, error: () => { this.loadingThreads = false; } }); } createNewChat(): void { this.threadService .createThread() .subscribe({ next: thread => { this.currentThreadId = thread.id; this.currentThreadTitle = thread.title; this.messages = []; this.loadThreads(); } }); } selectThread( thread: Thread ): void { this.currentThreadId = thread.id; this.currentThreadTitle = thread.title; this.messages = []; this.threadService .getMessages( thread.id ) .subscribe({ next: messages => { this.messages = messages; this.scrollToBottom(); } }); } sendMessage(): void { const text = this.messageText.trim(); if ( !text || !this.currentThreadId || this.loading ) { return; } this.messages.push({ role: 'user', content: text }); this.messageText = ''; this.loading = true; this.scrollToBottom(); this.chatService .sendMessage( this.currentThreadId, text ) .subscribe({ next: response => { this.messages.push({ role: 'assistant', content: response.message }); this.loading = false; this.loadThreads(); this.scrollToBottom(); }, error: error => { this.loading = false; this.messages.push({ role: 'assistant', content: 'Sorry, something went wrong.' }); console.error(error); } }); } resetChat(): void { /* * IMPORTANT: * * Reset does NOT delete the old * conversation. * * It creates a completely new * thread. */ this.createNewChat(); } logout(): void { this.auth.logout(); } deleteThread( event: Event, thread: Thread ): void { event.stopPropagation(); this.threadService .deleteThread( thread.id ) .subscribe({ next: () => { if ( this.currentThreadId === thread.id ) { this.currentThreadId = null; this.messages = []; } this.loadThreads(); } }); } onEnter( event: Event ): void { const keyboardEvent = event as KeyboardEvent; if ( keyboardEvent.shiftKey ) { return; } keyboardEvent.preventDefault(); this.sendMessage(); } trackByThread( index: number, thread: Thread ): string { return thread.id; } trackByMessage( index: number, message: ChatMessage ): number { return index; } scrollToBottom(): void { setTimeout(() => { const element = document.querySelector( '.messages' ); if (element) { element.scrollTop = element.scrollHeight; } }, 50); } } ============================================================ src/app/chat/chat.component.html ============================================================ <div class="chat-layout"> <!-- ==================================================== LEFT SIDEBAR ==================================================== --> <aside class="sidebar"> <div class="sidebar-header"> <div class="brand"> <div class="brand-icon"> ∑ </div> <div> <strong> Neural AI </strong> <small> Math Assistant </small> </div> </div> <button class="new-chat" (click)="createNewChat()" > <span>+</span> New Chat </button> </div> <div class="history-title"> Previous Chats </div> <div class="thread-list"> @if (loadingThreads) { <div class="empty-history"> Loading... </div> } @for ( thread of threads; track trackByThread($index, thread) ) { <div class="thread-item" [class.active]=" currentThreadId === thread.id " (click)="selectThread(thread)" > <div class="thread-icon"> 💬 </div> <div class="thread-info"> <div class="thread-title"> {{ thread.title }} </div> <div class="thread-date"> {{ thread.updated_at | date:'MMM d, h:mm a' }} </div> </div> <button class="delete-button" title="Delete" (click)=" deleteThread($event, thread) " > × </button> </div> } @if ( !loadingThreads && threads.length === 0 ) { <div class="empty-history"> No previous chats </div> } </div> <div class="sidebar-bottom"> <div class="user-info"> <div class="avatar"> {{ userEmail.charAt(0).toUpperCase() }} </div> <div class="user-email"> {{ userEmail }} </div> </div> <button class="logout" (click)="logout()" > ⇥ Logout </button> </div> </aside> <!-- ==================================================== MAIN CHAT ==================================================== --> <main class="main-chat"> <header class="chat-header"> <div> <h2> {{ currentThreadTitle }} </h2> <span> ReLU / Leaky ReLU Assistant </span> </div> <button class="reset" (click)="resetChat()" > ↻ Reset Chat </button> </header> <!-- ================================================== MESSAGES ================================================== --> <div class="messages"> @if ( messages.length === 0 ) { <div class="welcome"> <div class="welcome-icon"> ∑ </div> <h1> How can I help you? </h1> <p> Ask me to calculate ReLU or Leaky ReLU. </p> <div class="examples"> <button (click)=" messageText = 'Calculate ReLU(-5)' " > Calculate ReLU(-5) </button> <button (click)=" messageText = 'Calculate Leaky ReLU(-5)' " > Calculate Leaky ReLU(-5) </button> <button (click)=" messageText = 'Explain ReLU' " > Explain ReLU </button> </div> </div> } @for ( message of messages; track trackByMessage($index, message) ) { <div class="message-row" [class.user-row]=" message.role === 'user' " [class.ai-row]=" message.role === 'assistant' " > @if ( message.role === 'assistant' ) { <div class="message-avatar ai"> ∑ </div> } <div class="message-bubble" [class.user-bubble]=" message.role === 'user' " [class.ai-bubble]=" message.role === 'assistant' " > {{ message.content }} </div> @if ( message.role === 'user' ) { <div class="message-avatar user"> U </div> } </div> } @if (loading) { <div class="message-row ai-row"> <div class="message-avatar ai"> ∑ </div> <div class="message-bubble ai-bubble"> <div class="typing"> <span></span> <span></span> <span></span> </div> </div> </div> } </div> <!-- ================================================== INPUT ================================================== --> <div class="input-area"> <div class="input-box"> <textarea [(ngModel)]="messageText" placeholder="Ask about ReLU or Leaky ReLU..." rows="1" (keydown)="onEnter($event)" ></textarea> <button class="send" [disabled]=" !messageText.trim() || loading " (click)="sendMessage()" > ➤ </button> </div> <div class="input-hint"> Press Enter to send · Shift + Enter for new line </div> </div> </main> </div> ============================================================ src/app/chat/chat.component.css ============================================================ .chat-layout { height: 100vh; display: flex; background: #f8fafc; } /* ======================================================== SIDEBAR ======================================================== */ .sidebar { width: 280px; background: #111827; color: white; display: flex; flex-direction: column; } .sidebar-header { padding: 22px; } .brand { display: flex; align-items: center; gap: 12px; margin-bottom: 22px; } .brand-icon { width: 38px; height: 38px; border-radius: 10px; background: #374151; display: flex; align-items: center; justify-content: center; font-size: 22px; } .brand strong { display: block; } .brand small { display: block; color: #9ca3af; margin-top: 2px; } .new-chat { width: 100%; padding: 11px; border: 1px solid #4b5563; border-radius: 9px; background: #1f2937; color: white; cursor: pointer; font-size: 14px; } .new-chat:hover { background: #374151; } .history-title { padding: 10px 22px; color: #9ca3af; font-size: 12px; text-transform: uppercase; letter-spacing: .08em; } .thread-list { flex: 1; overflow-y: auto; padding: 5px 10px; } .thread-item { display: flex; align-items: center; gap: 10px; padding: 11px 12px; margin-bottom: 3px; border-radius: 8px; cursor: pointer; } .thread-item:hover { background: #1f2937; } .thread-item.active { background: #374151; } .thread-icon { font-size: 14px; } .thread-info { flex: 1; min-width: 0; } .thread-title { font-size: 13px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; } .thread-date { color: #9ca3af; font-size: 10px; margin-top: 3px; } .delete-button { border: 0; background: transparent; color: #6b7280; cursor: pointer; font-size: 18px; } .delete-button:hover { color: #f87171; } .empty-history { padding: 20px; text-align: center; color: #6b7280; font-size: 13px; } .sidebar-bottom { border-top: 1px solid #374151; padding: 15px; } .user-info { display: flex; align-items: center; gap: 10px; margin-bottom: 12px; } .avatar { width: 32px; height: 32px; border-radius: 50%; background: #374151; display: flex; align-items: center; justify-content: center; font-size: 12px; } .user-email { font-size: 12px; color: #d1d5db; overflow: hidden; text-overflow: ellipsis; } .logout { width: 100%; border: 0; background: transparent; color: #9ca3af; text-align: left; padding: 8px; cursor: pointer; } .logout:hover { color: white; } /* ======================================================== MAIN ======================================================== */ .main-chat { flex: 1; display: flex; flex-direction: column; min-width: 0; } .chat-header { height: 70px; border-bottom: 1px solid #e5e7eb; background: white; display: flex; align-items: center; justify-content: space-between; padding: 0 28px; } .chat-header h2 { margin: 0; font-size: 17px; color: #111827; } .chat-header span { font-size: 12px; color: #64748b; } .reset { border: 1px solid #e2e8f0; background: white; padding: 8px 14px; border-radius: 8px; cursor: pointer; } .reset:hover { background: #f1f5f9; } /* ======================================================== MESSAGES ======================================================== */ .messages { flex: 1; overflow-y: auto; padding: 30px 8%; } .message-row { display: flex; align-items: flex-start; gap: 10px; margin-bottom: 25px; } .user-row { justify-content: flex-end; } .message-avatar { width: 32px; height: 32px; flex-shrink: 0; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 12px; } .message-avatar.ai { background: #111827; color: white; } .message-avatar.user { background: #dbeafe; color: #1d4ed8; } .message-bubble { max-width: 70%; padding: 12px 16px; border-radius: 14px; white-space: pre-wrap; line-height: 1.6; font-size: 14px; } .ai-bubble { background: white; border: 1px solid #e5e7eb; color: #1f2937; } .user-bubble { background: #111827; color: white; } /* ======================================================== WELCOME ======================================================== */ .welcome { height: 100%; display: flex; flex-direction: column; align-items: center; justify-content: center; text-align: center; } .welcome-icon { width: 65px; height: 65px; border-radius: 18px; background: #111827; color: white; display: flex; align-items: center; justify-content: center; font-size: 30px; } .welcome h1 { margin: 20px 0 8px; color: #111827; } .welcome p { color: #64748b; } .examples { display: flex; gap: 10px; margin-top: 25px; } .examples button { padding: 9px 14px; border: 1px solid #e2e8f0; background: white; border-radius: 8px; cursor: pointer; } .examples button:hover { background: #f1f5f9; } /* ======================================================== TYPING ======================================================== */ .typing { display: flex; gap: 4px; } .typing span { width: 6px; height: 6px; border-radius: 50%; background: #94a3b8; animation: typing 1.2s infinite; } .typing span:nth-child(2) { animation-delay: .15s; } .typing span:nth-child(3) { animation-delay: .3s; } @keyframes typing { 0%, 60%, 100% { opacity: .3; transform: translateY(0); } 30% { opacity: 1; transform: translateY(-4px); } } /* ======================================================== INPUT ======================================================== */ .input-area { background: white; padding: 15px 8% 20px; border-top: 1px solid #e5e7eb; } .input-box { display: flex; align-items: flex-end; border: 1px solid #cbd5e1; border-radius: 13px; background: white; padding: 7px; } textarea { flex: 1; border: 0; outline: 0; resize: none; padding: 9px; font-family: inherit; font-size: 14px; } .send { width: 38px; height: 38px; border: 0; border-radius: 9px; background: #111827; color: white; cursor: pointer; } .send:disabled { opacity: .4; cursor: default; } .input-hint { text-align: center; margin-top: 7px; font-size: 11px; color: #94a3b8; } ============================================================ src/app/app.routes.ts ============================================================ import { Routes } from '@angular/router'; import { LoginComponent } from './login/login.component'; import { ChatComponent } from './chat/chat.component'; import { authGuard } from './core/auth.guard'; export const routes: Routes = [ { path: 'login', component: LoginComponent }, { path: 'chat', component: ChatComponent, canActivate: [ authGuard ] }, { path: '', redirectTo: 'chat', pathMatch: 'full' }, { path: '**', redirectTo: 'chat' } ]; ============================================================ src/app/app.config.ts ============================================================ import { ApplicationConfig } from '@angular/core'; import { provideRouter } from '@angular/router'; import { provideHttpClient, withInterceptors } from '@angular/common/http'; import { routes } from './app.routes'; import { authInterceptor } from './core/auth.interceptor'; export const appConfig: ApplicationConfig = { providers: [ provideRouter( routes ), provideHttpClient( withInterceptors([ authInterceptor ]) ) ] }; ============================================================ src/app/app.ts ============================================================ import { Component } from '@angular/core'; import { RouterOutlet } from '@angular/router'; @Component({ selector: 'app-root', standalone: true, imports: [ RouterOutlet ], template: ` <router-outlet /> ` }) export class App { } ============================================================ src/main.ts ============================================================ import { bootstrapApplication } from '@angular/platform-browser'; import { App } from './app/app'; import { appConfig } from './app/app.config'; bootstrapApplication( App, appConfig ).catch( err => console.error(err) ); ============================================================ src/styles.css ============================================================ * { box-sizing: border-box; } html, body { margin: 0; padding: 0; height: 100%; font-family: Inter, Arial, sans-serif; } body { background: #f8fafc; }